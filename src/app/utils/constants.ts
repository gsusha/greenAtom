const API_HEADERS = {
  'Content-Type': 'application/json',
};
const API_URL = 'https://api.choodoo.team';

// TODO: нахуй это
const onClick = () => {
  console.log(API_HEADERS);
  console.log(API_URL);
};

export default {
  API_HEADERS,
  API_URL,
  onClick,
};
